<h1>New group</h1>
<%= form_for @group, html: { multipart: true } do |f| %>
	<span class="picture">
		<%= f.file_field :picture %>
	</span>
	<div>
		<%= f.label :name %>
	</div>
	<div>
		<%= f.text_field :name %>
	</div>
	<div>
		<%= f.submit "Create Group" %>
	</div>
<% end %>